kind: Graph
name: blog
components:
  - name: ghost
    expose: ["http"]
    replicas: 1
  - name: mysql
  - name: redis
  - name: nginx
relations:
  - ["mysql:db", "ghost:db"]
  - ["redis:redis", "ghost:cache"]
  - ["nginx:http_proxy", "ghost:http_proxy"]
environment:
  name: dev
